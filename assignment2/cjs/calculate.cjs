// CommonJS File
const calculateArea = (length, width) => length * width;
const calculateVolume = (length, width, height) => length * width * height;
const calculateSquare = (length) => length * length;

module.exports = {calculateArea, calculateVolume, calculateSquare};
