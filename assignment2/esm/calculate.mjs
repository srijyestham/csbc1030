// ES6 File
let calculateArea = (length, width) => length * width;

let calculateVolume = (length, width, height) => length * width * height;

let calculateSquare = (length) => length * length;

export { calculateArea, calculateVolume, calculateSquare }
